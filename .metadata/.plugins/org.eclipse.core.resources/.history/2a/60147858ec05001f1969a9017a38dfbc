package com.kh.SocketEx;



public class ClinePre {
	public static void main(String[] args) {
		Socket  = new socket("localhost, ")
				
				
				
				
				//주최자로부터 받은 메세지 수신 및 출력
				String m = in.readLine();
				System.out.println("send succ");
				
				//for 문 while 사용해서 출력을 확인
				//클라이언트는 서버가 읽을 때 까지 여러번 메세지를 다시 보내서 출력 해볼수 있음
				for(int i = 0; i < 3; i++) {
					out.print();
					String t = in.readLine();
					System.out.println("send success : " + t);
				}
}
